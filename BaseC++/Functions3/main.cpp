#include <iostream>

using namespace std;

#pragma region Pointers
//int main() {
//
////    int arr[3]{1, 2, 3};
////
////    int index = 0;
////
////    cout << "Enter index of element: ";
////    cin >> index;
////
////    cout << *(arr + index) << endl;
////    cout << arr[2] << endl;
//
//    int num = 5;            // Создание обычной переменной
//    int *ptr_num = &num;    // Создание указателя и записывание туда адреса переменной выше
//
//    cout
//        << "Address of num:" << ' ' << &num << endl
//        << "Value of pointer:" << ' ' << ptr_num << endl
//        << "Value of num:" << num << endl
//        << "Value inside pointer:" << ' ' << *ptr_num << endl;
//
//    // & - reference, адресс
//    // * - derefeence, разименовывание адресв
//
//        return 0;
//}
#pragma endregion

#pragma region ArrayToArgument
/*

void fill_arr(int arr[], const int length) {
    for (int i = 0; i < length; ++i) {
        arr[i] = i + 1;
    }
}

void fill_arr2(int* arr, const int length) {
    for (int i = 0; i < length; ++i) {
        *(arr + i) = i + 1;
    }
}

void print_arr(int arr[], const int length) {
    for (int i = 0; i < length; ++i) {
        cout << arr[i] << ' ';
    }
}


int main() {


    int arr[5]{};

//    fill_arr(arr, 5);
    fill_arr2(arr, 5);
    print_arr(arr, 5);

    return 0;
}
*/

#pragma endregion



int main()
{



}
